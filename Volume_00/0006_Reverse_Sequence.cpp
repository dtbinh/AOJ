#include <iostream>
#include <vector>
#include <cmath>
#include <map>
#include <algorithm>
 
typedef long long ll;
using namespace std;
 
int main()
{
  string buf;
  cin >> buf;
  reverse(buf.begin(),buf.end());
  cout << buf << endl;
}
